---
kind: Route
apiVersion: v1
metadata:
  name: graph
  namespace: {{ graph_project }}
  annotations:
    kubernetes.io/tls-acme: "{{ graph_enable_acme }}"
spec:
  host: graph.{{ okd_route_basename }}
  to:
    kind: Service
    name: graph-http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
